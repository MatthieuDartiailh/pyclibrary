sudo: false

language: python
python:
    - 2.7
    - 3.2
    - 3.3
    - 3.4

install:
  - pip install -q pytest future pytest-cov pytest-capturelog
  - pip install -q coveralls

script:
  - py.test pyclibrary/tests -v --cov coveralls --cov-report term-missing

after_success:
  - coveralls;
